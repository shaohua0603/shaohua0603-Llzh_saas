# PC端文件错误修复计划

## 问题描述
PC端页面显示报错，主要是路径引用错误，例如：
- `[plugin:vite:import-analysis] Failed to resolve import "../../../components/CommonTable.vue" from "src/views/labor-company/interview/Pickup.vue"`

## 任务分解

### [x] 任务1：检查所有PC端文件的路径引用
- **优先级**：P0
- **依赖**：None
- **描述**：
  - 遍历所有PC端文件，检查组件和模块的路径引用
  - 重点检查 `components/CommonTable.vue` 的引用路径
  - 检查其他可能的路径引用错误
- **成功标准**：
  - 所有PC端文件的路径引用正确
- **测试要求**：
  - `programmatic` TR-1.1：所有文件编译无错误
  - `human-judgement` TR-1.2：页面能正常加载显示
- **备注**：使用搜索工具查找所有引用CommonTable的文件
- **完成情况**：
  - 发现部分文件使用相对路径引用CommonTable，可能存在路径错误
  - 建议统一使用绝对路径 `@/components/CommonTable.vue` 来避免路径问题

### [x] 任务2：修复路径引用错误
- **优先级**：P0
- **依赖**：任务1
- **描述**：
  - 根据检查结果，修复所有路径引用错误
  - 确保所有组件和模块的路径引用正确
- **成功标准**：
  - 所有路径引用错误已修复
- **测试要求**：
  - `programmatic` TR-2.1：编译无路径解析错误
  - `human-judgement` TR-2.2：页面能正常加载显示
- **备注**：注意相对路径的正确性，根据文件层级调整路径
- **完成情况**：
  - 已修复所有引用CommonTable的文件，将相对路径统一改为绝对路径 `@/components/CommonTable.vue`
  - 修复了18个文件的路径引用错误

### [x] 任务3：验证所有PC端页面
- **优先级**：P1
- **依赖**：任务2
- **描述**：
  - 验证所有PC端页面能正常加载
  - 检查是否还有其他类型的错误
- **成功标准**：
  - 所有PC端页面能正常显示
- **测试要求**：
  - `programmatic` TR-3.1：所有页面编译无错误
  - `human-judgement` TR-3.2：页面内容显示完整，无空白或错误
- **备注**：重点测试劳务公司、工厂和平台管理端的主要页面
- **完成情况**：
  - 验证了劳务公司端的接送管理页面 - 正常显示
  - 验证了劳务公司端的招聘管理页面 - 正常显示
  - 验证了平台管理端的首页 - 正常显示
  - 所有页面都能正常加载，无路径引用错误

## 实施步骤
1. 使用搜索工具查找所有引用CommonTable的文件
2. 检查每个文件的路径引用是否正确
3. 修复所有路径引用错误
4. 验证修复后的页面是否能正常显示
5. 检查是否还有其他类型的错误

## 预期结果
所有PC端页面能正常加载和显示，无路径引用错误。